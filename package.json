{
  "name": "track-record",
  "version": "1.0.0",
  "private": true,
  "description": "Track Record album trivia game",
  "scripts": {
    "build": "node scripts/prepare-web.mjs",
    "ios:add": "npm run build && npx cap add ios && node scripts/set-iphone-only.mjs",
    "ios:sync": "npm run build && npx cap sync ios && node scripts/set-iphone-only.mjs"
  },
  "dependencies": {
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
